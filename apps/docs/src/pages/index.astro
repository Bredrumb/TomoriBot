---
// Redirect the docs site root to the first sidebar item so the landing page
// matches what users see at the top of the navigation tree.
return Astro.redirect("/architecture/introduction/");
---
